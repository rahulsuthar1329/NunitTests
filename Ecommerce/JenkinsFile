pipeline {
    agent any

    stages {
        stage('Build Code')
        {
            steps{
                bat script: 'dotnet restore .'
                bat script: 'dotnet test'
            }
        }
    }
}
